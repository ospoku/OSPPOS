

@using OSPPOS.ViewComponents
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Identity
@using System.Web
@using Microsoft.AspNetCore.DataProtection
@using OSPPOS.Models
@using OSPPOS.ViewModels
@inject UserManager<AppUser> usm;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model IEnumerable<OSPPOS.ViewModels.ViewCustomersVM>;
@inject Microsoft.AspNetCore.DataProtection.IDataProtectionProvider DataProtectionProvider

@{
    // Optional: create protector in view (same purpose string)
    var protector = DataProtectionProvider.CreateProtector("IdProtector");
  
}


<div class="tab-content tab-content-dim" id="myTabContent">
    <!-- TAB 1: USERS -->
    <div class="tab-pane fade show active tab-pane-dim" id="users" role="tabpanel">
        <div class="tab-card">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h6 class="fw-semibold mb-0"><i class="bi bi-people me-2"></i>Customers</h6>
                <button class="btn btn-primary btn-sm" id="openFormBtn2" onclick="document.getElementById('AddCustomerDialog').showModal()">
                    <i class="bi bi-plus-circle me-1"></i>Add User
                </button>
            </div>
            <!-- DATATABLES -->
            <div class="table-container">
                <table id="customerTable" class="table darkTable">
                    <thead>
                        <tr>
                            
                            <th>Name</th>
                            <th>Phone</th>
                            <th>Email</th>
                            <th>Address</th>
                            <th>Tax Number</th>
                            <th>IsActive</th>
                            <th>Allow Credit</th>
                            <th>Credit Limit</th>
                            <th>Sales Order</th>
                            <th>Payments</th>
                            <th>Total Debt</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                                     @foreach (var customer in Model)
                {
                        <tr>
                            @* <td><div class="d-flex align-items-center gap-2"><span class="table-avatar">JD</span><span class="fw-medium">John Doe</span></div></td> *@
                           
                       
                            
                                <td class="ellipsed-text">@customer.Name</td>
                            <td class="ellipsed-text"> @customer.Phone</td>

                            <td class="ellipsed-text">@customer.Email</td>
                            <td class="ellipsed-text">
                                @customer.Address
                            </td>
                            <td>@customer.TaxNumber</td>
                            <td ><span class="status-badge status-active">@customer.IsActive</span></td>
                            <td>@customer.AllowCredit</td>
                                <td>@customer.CreditLimit</td>
                                <td>@customer.SaleOrders.Count()</td>
                                <td>@customer.Payments.Count()</td>
                                <td>@customer.TotalDebt</td>
                            <td class="text-end">
                                <button class="table-action-btn text-primary" title="Edit"><i class="bi bi-pencil"></i></button>
                                <button class="table-action-btn text-danger" title="Delete"><i class="bi bi-trash"></i></button>
                                    @if (customer.CanEdit)
                                    {

                                        <button onclick="openDialogFromData(this)" data-url="EditCustomer" data-user-id="@protector.Protect(customer.PublicId.ToString())" data-dialog-id="EditCustomerDialog" class="dropdown-item-custom"><i style="width:15px; height:15px" class=" fa fa-edit mr-3 text-warning"></i>Edit</button>
                                    }



                                    @if (customer.CanPrint)
                                    {

                                        <button type="button" class="dropdown-item-custom" onclick="printcustomerIframe('@protector.Protect(customer.PublicId.ToString())')">
                                            <i style="width:15px; height:15px" class="fa fa-print mr-3 text-primary"></i>Print
                                        </button>
                                    }


                                    <button type="button" onclick="de" class="dropdown-item-custom"><i style="width:15px; height:15px" class="fa fa-trash mr-3 text-danger"></i>Delete</button>

                            </td>
                        </tr>
                        }
                  
                           
                 
               
                    </tbody>
                </table>
            </div>
        </div>
    </div>

  </div>

    @await Component.InvokeAsync(nameof(AddCustomer))

    <dialog id="EditCustomerDialog">
        <div id="EditCustomerDialogContent"></div>
    </dialog>

<iframe id="printFrame" style="display:none;"></iframe>

<script type="text/javascript">
    function printcustomerIframe(customerId) {
        const printFrame = document.getElementById('printFrame');
        printFrame.src = `/customer/Printcustomer?Id=${customerId}`;
        printFrame.onload = function () {
            printFrame.contentWindow.focus();
            printFrame.contentWindow.print();
        };
    }

</script>


